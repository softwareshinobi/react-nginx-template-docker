services:

    react-nginx-template:

        container_name: react-nginx-template

        image: softwareshinobi/react-nginx-template

        build: 

            context: .

            dockerfile: Dockerfile

        ports:

            - 8000:80

            - 80:80
